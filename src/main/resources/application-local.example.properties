# Use this file as a template to create application-local.properties file to store your local db credentials
spring.datasource.url=${DB_URL:jdbc:postgresql://order_db:5432/order_service}
spring.datasource.username=${DB_USER:your_local_db_username_here}
spring.datasource.password=${DB_PASS:your_local_db_password_here}